func: quote(loadingNamespaceInfo)
body: function () 
body: {
body:     dynGet <- function(name, notFound = stop(gettextf("%s not found", sQuote(name)), domain = NA)) {
body:         n <- sys.nframe()
body:         while (n > 1) {
body:             n <- n - 1
body:             env <- sys.frame(n)
body:             if (exists(name, envir = env, inherits = FALSE)) 
body:                 return(get(name, envir = env, inherits = FALSE))
body:         }
body:         notFound
body:     }
body:     dynGet("__LoadingNamespaceInfo__", stop("not loading a namespace"))
body: }
args: NULL
retn: structure(list(libname = "/home/roman/r-instrumented/library", pkgname = "utils"), .Names = c("libname", "pkgname"))

