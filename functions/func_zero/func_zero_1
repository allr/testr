func: quote(zero)
body: function (f, guesses, tol = 1e-07) 
body: {
body:     f.check <- function(x) {
body:         x <- f(x)
body:         if (!is.numeric(x)) 
body:             stop("Need a numeric result")
body:         as.double(x)
body:     }
body:     .Call("zero", body(f.check), as.double(guesses), as.double(tol), new.env())
body: }
args: list(function (x) 
args: (x^2 + 1) * (x - 1.5), c(0, 5))
retn: 1.50000002235174

