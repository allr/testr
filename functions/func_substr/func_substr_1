func: quote(substr)
body: function (x, start, stop) 
body: {
body:     if (!is.character(x)) 
body:         x <- as.character(x)
body:     .Internal(substr(x, as.integer(start), as.integer(stop)))
body: }
args: list("éè", 1, 1)
retn: "é"

