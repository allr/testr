func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.62", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.63", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.64", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.65", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.66", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.67", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.61", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.60", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.69", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.70", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.71", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.72", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.73", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.74", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.68", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.76", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.77", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.78", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.79", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.80", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.81", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.75", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.83", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.84", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.85", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.86", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.87", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.88", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.82", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.90", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.91", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.92", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.93", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.94", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.95", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.89", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.97", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.98", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.99", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.100", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.101", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.102", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.96", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.104", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.105", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.106", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.107", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.108", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.109", envir = <environment>, inherits = FALSE)
retn: NULL

func: quote(remove)
body: function (..., list = character(), pos = -1, envir = as.environment(pos), inherits = FALSE) 
body: {
body:     dots <- match.call(expand.dots = FALSE)$...
body:     if (length(dots) && !all(sapply(dots, function(x) is.symbol(x) || is.character(x)))) 
body:         stop("... must contain names or character strings")
body:     names <- sapply(dots, as.character)
body:     if (length(names) == 0L) 
body:         names <- character()
body:     list <- .Primitive("c")(list, names)
body:     .Internal(remove(list, envir, inherits))
body: }
args: list("GRID.VP.103", envir = <environment>, inherits = FALSE)
retn: NULL

