func: quote(var)
body: function (x, y = NULL, na.rm = FALSE, use) 
body: {
body:     if (missing(use)) 
body:         use <- if (na.rm) 
body:             "na.or.complete"
body:         else "everything"
body:     na.method <- pmatch(use, c("all.obs", "complete.obs", "pairwise.complete.obs", "everything", "na.or.complete"))
body:     if (is.na(na.method)) 
body:         stop("invalid 'use' argument")
body:     if (is.data.frame(x)) 
body:         x <- as.matrix(x)
body:     else stopifnot(is.atomic(x))
body:     if (is.data.frame(y)) 
body:         y <- as.matrix(y)
body:     else stopifnot(is.atomic(y))
body:     .Call(C_cov, x, y, na.method, FALSE)
body: }
args: list(c(46, 43))
retn: 4.5

func: quote(var)
body: function (x, y = NULL, na.rm = FALSE, use) 
body: {
body:     if (missing(use)) 
body:         use <- if (na.rm) 
body:             "na.or.complete"
body:         else "everything"
body:     na.method <- pmatch(use, c("all.obs", "complete.obs", "pairwise.complete.obs", "everything", "na.or.complete"))
body:     if (is.na(na.method)) 
body:         stop("invalid 'use' argument")
body:     if (is.data.frame(x)) 
body:         x <- as.matrix(x)
body:     else stopifnot(is.atomic(x))
body:     if (is.data.frame(y)) 
body:         y <- as.matrix(y)
body:     else stopifnot(is.atomic(y))
body:     .Call(C_cov, x, y, na.method, FALSE)
body: }
args: list(c(61, 64, 70, 49, 41, 59))
retn: 111.466666666667

func: quote(var)
body: function (x, y = NULL, na.rm = FALSE, use) 
body: {
body:     if (missing(use)) 
body:         use <- if (na.rm) 
body:             "na.or.complete"
body:         else "everything"
body:     na.method <- pmatch(use, c("all.obs", "complete.obs", "pairwise.complete.obs", "everything", "na.or.complete"))
body:     if (is.na(na.method)) 
body:         stop("invalid 'use' argument")
body:     if (is.data.frame(x)) 
body:         x <- as.matrix(x)
body:     else stopifnot(is.atomic(x))
body:     if (is.data.frame(y)) 
body:         y <- as.matrix(y)
body:     else stopifnot(is.atomic(y))
body:     .Call(C_cov, x, y, na.method, FALSE)
body: }
args: list(c(60, 51))
retn: 40.5

func: quote(var)
body: function (x, y = NULL, na.rm = FALSE, use) 
body: {
body:     if (missing(use)) 
body:         use <- if (na.rm) 
body:             "na.or.complete"
body:         else "everything"
body:     na.method <- pmatch(use, c("all.obs", "complete.obs", "pairwise.complete.obs", "everything", "na.or.complete"))
body:     if (is.na(na.method)) 
body:         stop("invalid 'use' argument")
body:     if (is.data.frame(x)) 
body:         x <- as.matrix(x)
body:     else stopifnot(is.atomic(x))
body:     if (is.data.frame(y)) 
body:         y <- as.matrix(y)
body:     else stopifnot(is.atomic(y))
body:     .Call(C_cov, x, y, na.method, FALSE)
body: }
args: list(c(40, 62, 56, 61, 49))
retn: 84.3

func: quote(var)
body: function (x, y = NULL, na.rm = FALSE, use) 
body: {
body:     if (missing(use)) 
body:         use <- if (na.rm) 
body:             "na.or.complete"
body:         else "everything"
body:     na.method <- pmatch(use, c("all.obs", "complete.obs", "pairwise.complete.obs", "everything", "na.or.complete"))
body:     if (is.na(na.method)) 
body:         stop("invalid 'use' argument")
body:     if (is.data.frame(x)) 
body:         x <- as.matrix(x)
body:     else stopifnot(is.atomic(x))
body:     if (is.data.frame(y)) 
body:         y <- as.matrix(y)
body:     else stopifnot(is.atomic(y))
body:     .Call(C_cov, x, y, na.method, FALSE)
body: }
args: list(c(49, 61, 58, 52))
retn: 30

func: quote(var)
body: function (x, y = NULL, na.rm = FALSE, use) 
body: {
body:     if (missing(use)) 
body:         use <- if (na.rm) 
body:             "na.or.complete"
body:         else "everything"
body:     na.method <- pmatch(use, c("all.obs", "complete.obs", "pairwise.complete.obs", "everything", "na.or.complete"))
body:     if (is.na(na.method)) 
body:         stop("invalid 'use' argument")
body:     if (is.data.frame(x)) 
body:         x <- as.matrix(x)
body:     else stopifnot(is.atomic(x))
body:     if (is.data.frame(y)) 
body:         y <- as.matrix(y)
body:     else stopifnot(is.atomic(y))
body:     .Call(C_cov, x, y, na.method, FALSE)
body: }
args: list(c(60, 61))
retn: 0.5

func: quote(var)
body: function (x, y = NULL, na.rm = FALSE, use) 
body: {
body:     if (missing(use)) 
body:         use <- if (na.rm) 
body:             "na.or.complete"
body:         else "everything"
body:     na.method <- pmatch(use, c("all.obs", "complete.obs", "pairwise.complete.obs", "everything", "na.or.complete"))
body:     if (is.na(na.method)) 
body:         stop("invalid 'use' argument")
body:     if (is.data.frame(x)) 
body:         x <- as.matrix(x)
body:     else stopifnot(is.atomic(x))
body:     if (is.data.frame(y)) 
body:         y <- as.matrix(y)
body:     else stopifnot(is.atomic(y))
body:     .Call(C_cov, x, y, na.method, FALSE)
body: }
args: list(c(69, 42, 65, 46, 58))
retn: 137.5

func: quote(var)
body: function (x, y = NULL, na.rm = FALSE, use) 
body: {
body:     if (missing(use)) 
body:         use <- if (na.rm) 
body:             "na.or.complete"
body:         else "everything"
body:     na.method <- pmatch(use, c("all.obs", "complete.obs", "pairwise.complete.obs", "everything", "na.or.complete"))
body:     if (is.na(na.method)) 
body:         stop("invalid 'use' argument")
body:     if (is.data.frame(x)) 
body:         x <- as.matrix(x)
body:     else stopifnot(is.atomic(x))
body:     if (is.data.frame(y)) 
body:         y <- as.matrix(y)
body:     else stopifnot(is.atomic(y))
body:     .Call(C_cov, x, y, na.method, FALSE)
body: }
args: list(c(59, 54, 48, 48))
retn: 28.25

